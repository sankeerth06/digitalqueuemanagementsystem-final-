# When deployed separately (e.g. Vercel + Render), point this at your backend's public URL.
# Left blank in local dev, since vite.config.ts proxies /api and /socket.io to localhost:5000.
VITE_API_BASE_URL=
